# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = ["cd backend && pip install --upgrade pip && pip install -r requirements.txt"]

[start]
cmd = "cd backend && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 300"
